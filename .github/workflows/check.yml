name: CI Pipeline

on:
  push:
    branches: [ main, backend_develop, frontend_develop ]
  pull_request:
    branches: main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Create .env file from GitHub Secrets
        run: |
          cat > .env << 'EOF'
          DATABASE_URL=${{ secrets.DATABASE_URL }}
          BOT_TOKEN=${{ secrets.BOT_TOKEN }}
          EOF
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build Docker images
        run: docker compose --env-file .env build
      - name: Prepare reports directory
        run: mkdir -p reports/api
      - name: Start PostgreSQL and wait for health
        run: |
<<<<<<< HEAD
          docker compose --env-file .env run --rm api pytest tests/unit \
            -v \
            --cov=. \
            --cov-report=term-missing \
            --cov-report=html:/app/reports/htmlcov \
            --cov-report=xml:/app/reports/coverage.xml \
            --cov-fail-under=80 \
            --cov-config=/app/.coveragerc \
            --junitxml=/app/reports/junit.xml

      # ==================== Bot Tests ====================
      - name: Run Bot unit tests
=======
          docker compose up -d postgres
          while [ "$(docker inspect --format='{{.State.Health.Status}}' $(docker compose ps -q postgres))" != "healthy" ]; do
          sleep 2
          done
      - name: Run API integration tests
>>>>>>> df8fccd5c237930238f52f7ac56cb2dffd051506
        run: |
          docker compose run --rm api pytest api/tests/test_integration.py -v --junitxml=reports/api/junit.xml
      - name: Run API integration tests for bot
        run: |
          docker compose run --rm api pytest api/tests/test_integration_bot.py -v --junitxml=reports/api/junit-bot.xml
      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: reports/
          retention-days: 7
